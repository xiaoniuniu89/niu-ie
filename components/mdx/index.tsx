import type { MDXComponents } from "mdx/types";

export const mdxComponents: MDXComponents = {
  // Add custom MDX components here over time.
  // Example:
  // YouTube: ({ id }: { id: string }) => (
  //   <iframe src={`https://www.youtube.com/embed/${id}`} ... />
  // ),
};
